tutoring.alexbur.net {
  encode gzip

  # Proxy API requests to Flask backend
  @api {
    path /api/*
  }
  reverse_proxy @api backend:5000

  # All other requests go to frontend
  reverse_proxy frontend:80
}